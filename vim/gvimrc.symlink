set anti
set guifont=Anonymous\ Pro:h20
set lines=999 columns=84

" a: Autoselect VISUAL mode 
" A: Autoselect modeless
" c: console dialog instead of popup for simple things
" e: tab pages something or other
" g: Grey menu items: Make menu items that are not active grey.
" m: Menu bar is present.
" r: Right-hand scrollbar is always present.
" t: Include tearoff menu items.
set guioptions=aAcegmrt

" Command + t on macvim
" Control + t otherwise
" Also, Command +s is :w

if has("gui_macvim")
  macmenu &File.New\ Tab key=<nop>
  map <unique> <silent> <D-t> <Plug>PeepOpen
  map <unique> <silent> ` <Plug>PeepOpen
  map <D-s> :w<cr>
else
  " We don't have CommandT anymore, maybe find an alternative?
  " map <c-t> :CommandT<CR>
  map <c-s> :w<cr>
endif

" Open Command+T in a new tab
let g:CommandTAcceptSelectionTabMap='<CR>'

" C-TAB and C-SHIFT-TAB cycle tabs forward and backward
nmap <c-tab> :tabnext<cr>
imap <c-tab> <c-o>:tabnext<cr>
vmap <c-tab> <c-o>:tabnext<cr>
nmap <c-s-tab> :tabprevious<cr>
imap <c-s-tab> <c-o>:tabprevious<cr>
vmap <c-s-tab> <c-o>:tabprevious<cr>

" C-# switches to tab
nmap <d-1> 1gt
nmap <d-2> 2gt
nmap <d-3> 3gt
nmap <d-4> 4gt
nmap <d-5> 5gt
nmap <d-6> 6gt
nmap <d-7> 7gt
nmap <d-8> 8gt
nmap <d-9> 9gt
